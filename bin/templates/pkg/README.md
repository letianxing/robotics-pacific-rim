# pkg-{{name}}

Pure shared package for {{title}}.

Keep this package transport-neutral and runtime-neutral. Concrete adapters,
processes, deployment manifests, and business-specific rules should live in
`infra/*`, `module/*`, or `deploy/*`.
