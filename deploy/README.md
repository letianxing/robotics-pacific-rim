# deploy

Deployment topology and environment overlays.

Deployment projects describe how modules run together. They can depend on
modules and shared packages, but runtime code should not depend on deployment
projects.
