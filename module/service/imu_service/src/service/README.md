Use-case service implementations live here.

This pure branch keeps `imu_service` as a minimal driver-shaped sample. Real
IMU acquisition, hardware adapters, scheduler loops, and publishing behavior
have intentionally been removed.
