# infra-runtime

Runtime lifecycle and node composition interfaces for modules.

This project depends on `infra-protocol` for message boundaries and should remain
usable by any robot capability module.
